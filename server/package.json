{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "App.js",
  "scripts": {
    "start:server": "nodemon server.js",
    "start": "(cd ../client && yarn run build) && (cd ../server && nodemon server.js)",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2",
    "express": "^4.18.3",
    "nodemon": "^3.1.0",
    "path": "^0.12.7"
  }
}
